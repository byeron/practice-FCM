2/2
FCSデータのパーサーを試した
過去のコードを確認しながら進めたが、おそらく回りくどい実装をしていたので
データの中身を確認しながら、別のアプローチで実装した。
データのフォーマットについて、ドキュメントが見当たらず類推しながら進めた。

fcsparserでパースを行うと、データとメタデータが得られる。メタデータを使うことで
金属同位体とタンパク質をバインドすることができる。
メタデータはdict型で、
タンパク質名は`$PnS`（nは数値）に格納されている
金属同位体は`_channel_name`に格納されており、インデックス番号が統一されているらしい
これをもとにdataの列名にタンパク質を表記する実装ができた
